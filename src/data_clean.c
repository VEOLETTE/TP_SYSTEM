#include <stdio.h>
#include "data.h"

// Prototype : à toi de l'améliorer selon min_user / min_item
int clean_ratings(Rating* ratings, int count, int min_user, int min_item) {
    // À implémenter : filtrage par fréquence
    // Ex : compter les occurrences de chaque user_id et item_id
    // et retirer ceux en dessous des seuils

    // Retourner le nombre d'éléments restants après nettoyage
    return count;
}
